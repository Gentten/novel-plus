#端口号
server:
  port: 8085

#不分表的数据库配置
spring:
  datasource:
    url: jdbc:mysql://192.168.174.129:3306/novel_plus?allowPublicKeyRetrieval=true&useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: 123jiayouA!
  redis:
    host: 192.168.174.129
    port: 6379
    password:
  profiles:
    include: [ common-dev ]
####使用shardingJdbc时，
####所有的jdbcType都不能是LONGVARCHAR,否则会导致java.io.NotSerializableException: java.io.StringReader错误
##### 应该替换所有的 LONGVARCHAR 类型为VARCHAR
sharding:
  jdbc:
    datasource:
      ds0:
        jdbc-url: jdbc:mysql://192.168.174.129:3306/novel_plus?allowPublicKeyRetrieval=true&useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
        username: root
        password: 123jiayouA!

pic:
  save:
    type: 1 #图片保存方式, 1不保存，使用爬取的网络图片 ，2保存在自己的存储介质
    storage: local #存储介质，local：本地，OSS：阿里云对象存储
    path: /Users/xiongxiaoyang/java/  #图片保存路径

#模版配置
templates:
  name: green

thread:
  pool:
    #核心线程池数量
    core-pool-size: 10
    #最大线程池数量
    maximum-pool-size: 20
    #线程超时时间（秒）
    keep-alive-time: 10
    #队列大小
    queue-size: 100

