<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.java2nb.novel.mapper.BookRankMapper">


    <resultMap id="BookRankResultMap" type="com.java2nb.novel.entity.BookRank">
        <id property="id" column="id"/>
        <result property="bookId" column="book_id"/>
        <result property="bookName" column="book_name"/>
        <result property="author" column="author"/>
        <result property="catId" column="cat_id"/>
        <result property="rankType" column="rank_type"/>
        <result property="rankValue" column="rank_value"/>
        <result property="rankDate" column="rank_date"/>
        <result property="rankOrder" column="rank_order"/>
        <result property="extInfo" column="ext_info"/>
    </resultMap>

    <insert id="insert" parameterType="com.java2nb.novel.entity.BookRank" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO book_rank (book_id, book_name, author, cat_id, rank_type, rank_value, rank_date, rank_order, ext_info)
        VALUES (#{bookId},#{bookName}, #{author},  #{catId}, #{rankType}, #{rankValue}, #{rankDate}, #{rankOrder}, #{extInfo})
    </insert>

    <select id="selectById" parameterType="long" resultMap="BookRankResultMap">
        SELECT * FROM book_rank WHERE id = #{id}
    </select>

    <select id="selectAll" resultMap="BookRankResultMap">
        SELECT * FROM book_rank
    </select>

    <update id="updateRank" parameterType="com.java2nb.novel.entity.BookRank">
        UPDATE book_rank
        SET book_id = #{bookId},
            book_name = #{bookName},
            author = #{author},
            cat_id = #{catId},
            rank_type = #{rankType},
            rank_value = #{rankValue},
            rank_date = #{rankDate},
            rank_order = #{rankOrder},
            ext_info = #{extInfo}
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="long">
        DELETE FROM book_rank WHERE id = #{id}
    </delete>



    <insert id="batchInsert" parameterType="java.util.List">
        insert into book_rank (id, book_id,book_name, author, cat_id, rank_type, rank_value, rank_date, rank_order,
        ext_info)
        values
        <foreach collection="list" item="item" separator=",">
            (
            #{item.id},
            #{item.bookId},
            #{item.bookName},
            #{item.author},
            #{item.catId},
            #{item.rankType},
            #{item.rankValue},
            #{item.rankDate},
            #{item.rankOrder},
            #{item.extInfo}
            )
        </foreach>
    </insert>




</mapper>